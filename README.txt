To use the program, you must first have a development environment configured with pyhton to be able to execute the following command:

```
python count_characters.py [replace this placeholder with the character to count]
```
